version: '3'
services:
  blinkt:
    image: phriscage/iot_sys-blinkt
    #cap-add:
      #- SYS_RAWIO
    #devices:
      #- /dev/mem
    volumes:
      - /sys:/sys
    ports:
      - 8080:8080
    #networks:
      #- front
    deploy:
      placement:
        constraints:
          - node.role != manager
#networks:
  #front:
